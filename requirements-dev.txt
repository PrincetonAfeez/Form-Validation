# Dev, test, lint, and E2E. Install: pip install -r requirements-dev.txt
-r requirements.txt

# Testing
pytest>=8.3,<9.0
pytest-django>=4.9,<5.0
pytest-cov>=5.0,<7.0
coverage>=7.6,<8.0
hypothesis>=6.120,<7.0

# Browser E2E (CI: python -m playwright install --with-deps chromium)
playwright>=1.49,<2.0
pytest-playwright>=0.5,<0.7

# Lint / format
ruff>=0.8,<0.10
black>=24.10,<26.0
